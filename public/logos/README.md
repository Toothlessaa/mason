# Logo Assets

Place the two logo files here and keep these names:

- `district-grand-lodge.png`
- `mt-capistrano-lodge.png`

The current page uses graceful placeholder seals until the real logo files are added.
